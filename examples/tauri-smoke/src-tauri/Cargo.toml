[package]
name = "release-qa-tauri-smoke"
version = "0.1.0"
description = "Independent packaged Tauri consumer used to prove Release QA automation"
edition = "2021"
publish = false

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
